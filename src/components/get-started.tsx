import PrimaryButton from "@/components/primary-button";

export const GetStarted = () => {
  return (
    <PrimaryButton isLink={true} href="/login">
      Get Started
    </PrimaryButton>
  );
};

export default GetStarted;
